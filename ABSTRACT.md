# Abstract：TCIR 台风强度识别框架（含优势创新与局限）

热带气旋强度实时定强高度依赖主观经验且夜间受限。本项目基于 PyTorch 构建了一个配置驱动、面向 TCIR 卫星数据集的热带气旋强度识别框架，通过多通道卫星云图（IR1/WV/PMW）端到端回归最大持续风速 Vmax（knot）。框架以“配置驱动 + 可插拔多通道骨干”为核心，支持零代码切换 ResNet/VGG16/EfficientNet/ViT 等骨干与 3/4 通道输入；采用**按风暴 ID 防泄漏切分**、逐通道归一化、HDF5 二级数据管线（懒加载 + 紧凑抽取）与 AMP 混合精度训练。在西北太平洋（WPAC，2913 测试样本）上取得 **RMSE 16.22 kn、MAE 11.93 kn、R² 0.76**，逼近 best-track 约 10 kn 标注噪声下限，且数据自带标签、无需人工标注。

## 优势与创新
1. 配置驱动、调参零代码侵入，骨干首层随通道数自动重建；
2. by-storm 防泄漏切分保证评估真实可信；
3. 领域正确的逐通道预处理，默认弃用昼夜不稳定的 VIS；
4. 二级数据管线化解 30GB HDF5 与 WSL 跨盘 I/O 瓶颈；
5. 配套误差归因（worst-12→风暴 ID）、可视化与 TCIR 适配推理链。

## 显著缺点（与现有台风强度分类/定强模型比较）
1. **回归范式在极端强度段系统性低估**：误差分析显示 Cat 4–5 强台风被显著向均值拉低（12 个最差样本中有 11 个为强台风，signed error 达 −60 ~ −81 kn）。而现有 TCIR 原论文及多数业务定强模型采用**强度分级（分类）**或分段/分位数回归，对极端个例更稳健，也更契合预警分级需求——这是本回归范式相对分类模型最明显的短板。
2. **单帧快照、无时序与多源融合**：本模型只用单时刻云图；业务级 CIMSS ADT / SATCON 等融合 IR、被动微波与 AMSU 微波探测及**时间演变**（Dvorak 核心技术），对快速增强/减弱更敏感。本模型对快速变化的台风捕获能力弱。
3. **仅覆盖西北太平洋（WPAC）**：现有多洋区模型（ATL/EPAC/WPAC 联合训练）具备跨洋区泛化；WPAC-only 训练在大西洋等形态差异大的洋区泛化性未验证。
4. **未与业务基准对标**：缺少与持续性/气候持续性（persistence/CLIPER）、官方 ADT 的**技能评分（skill score）**对比——仅有 RMSE/MAE 不足以界定其相对业务系统的增益与可用边界。
5. **PMW 通道的近实时可用性问题**：被动微波由极轨卫星提供，时间覆盖稀疏（约 2 次/日），限制了近实时业务化；完全依赖该通道的输入在多数时刻不可得。
6. **骨干与任务偏常规、无不确定性估计**：ResNet18/VGG16 为通用视觉骨干，未引入旋转/尺度不变的气象专用结构；输出为单点估计，无预测区间。
7. **已逼近标签噪声下限**：RMSE 16.2 kn 已接近 best-track ~10 kn 噪声，意味着在“同标签体系”下继续提升有限；进一步突破需更优特征或多源/时序输入，而非单纯加深网络。

## 结论
该框架在工程可用性、可复现性与零标注训练上具有明显优势，适合作为多洋区/多骨干定强研究的**基线平台**；其单帧回归范式在极端强度与快速演变个例上不及分类/时序融合的业务模型，宜作为辅助参考而非替代业务定强。
